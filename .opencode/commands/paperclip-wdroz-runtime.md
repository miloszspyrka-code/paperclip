---
description: Execute an auditable Paperclip runtime change through a normal task workflow
---
Load and follow the `paperclip-wdroz-runtime` skill.

User request / arguments:

$ARGUMENTS

Do not write an untracked side change. Require an existing or newly created Paperclip task first.
