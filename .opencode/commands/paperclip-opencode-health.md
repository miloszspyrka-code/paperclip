---
description: Diagnose Paperclip's isolated OpenCode runtime, provider, plugins and sessions
---
Load and follow the `paperclip-opencode-health` skill.

User request / arguments:

$ARGUMENTS

Never expose secrets and never inherit the host OpenCode configuration.
