export const CHATGPT_PUBLIC_TOOL_NAMES = [
  "paperclipMe",
  "paperclipListAgents",
  "paperclipGetAgent",
  "paperclipGetAgentApps",
  "paperclipListAgentAppBindings",
  "paperclipUpdateAgent",
  "paperclipListIssues",
  "paperclipGetIssue",
  "paperclipGetHeartbeatContext",
  "paperclipListComments",
  "paperclipListIssueInteractions",
  "paperclipListIssueApprovals",
  "paperclipGetApproval",
  "paperclipListProjects",
  "paperclipGetProject",
  "paperclipListGoals",
  "paperclipGetIssueWorkspaceRuntime",
  "paperclipControlIssueWorkspaceServices",
  "paperclipWaitForIssueWorkspaceService",
  "paperclipCreateIssue",
  "paperclipUpdateIssue",
  "paperclipAddComment",
  "paperclipResolveIssueInteraction",
  "paperclipApprovalDecision",
  "paperclipCancelHeartbeatRun",
  "paperclipListDocuments",
  "paperclipGetDocument",
  "paperclipGetDocumentHistory",
  "paperclipGetDocumentRevision",
  "paperclipUpdateDocument",
  "paperclipWikiList",
  "paperclipWikiSearch",
  "paperclipWikiGetPage",
  "paperclipWikiGetMetadata",
  "paperclipWikiProposeChange",
  "paperclipWikiApplyChange",
  "paperclipListSkills",
  "paperclipGetSkill",
  "paperclipUseSkill",
  "paperclipListIssueRuns",
  "paperclipGetRunEvents",
  "paperclipGetRunMetrics",
  "paperclipListSecrets",
  "paperclipCreateSecret",
  "paperclipUpdateSecret",
  "paperclipRotateSecret",
  "paperclipDeleteSecret",
  "paperclipListToolApplications",
  "paperclipCreateToolApplication",
  "paperclipUpdateToolApplication",
  "paperclipListToolConnections",
  "paperclipGetToolConnection",
  "paperclipCreateToolConnection",
  "paperclipUpdateToolConnection",
  "paperclipTestToolConnection",
  "paperclipRefreshToolConnectionCatalog",
  "paperclipListToolProfiles",
  "paperclipGetToolProfile",
  "paperclipCreateToolProfile",
  "paperclipUpdateToolProfile",
  "paperclipBindToolProfile",
  "paperclipSetAgentAppPermission",
  "paperclipSetAgentAppInstallPolicy",
  "paperclipListToolGateways",
  "paperclipCreateToolGateway",
  "paperclipUpdateToolGateway",
  "paperclipGetExecutionWorkspaceDelivery",
  "paperclipPrepareIssueDelivery",
  "paperclipCreateIssuePullRequest",
  "paperclipMergeIssuePullRequest",
];

export const CHATGPT_PUBLIC_TOOL_NAME_SET = new Set(CHATGPT_PUBLIC_TOOL_NAMES);

export const CHATGPT_PUBLIC_TOOL_DESCRIPTIONS = {
  paperclipMe: "Get the authenticated Paperclip principal type (agent or Board) and company access. Use before governed mutations to avoid assuming unavailable authority.",
  paperclipListAgents: "List Paperclip agents with compact role, status, and reporting information.",
  paperclipGetAgent: "Get detailed role, runtime, and configuration information for one Paperclip agent.",
  paperclipGetAgentApps: "Get safe App/MCP permission and every-run installation assignments for one Paperclip agent.",
  paperclipListAgentAppBindings: "List safe App/MCP permission and every-run installation bindings with optional company, agent, and app filters.",
  paperclipUpdateAgent: "Update agent status (use active to resume), identity, role, or runtime configuration. Agent principals need agents:configure or agents:suggest-changes; the API enforces permission.",
  paperclipListIssues: "List Paperclip tasks/issues using filters such as status, project, assignee, or search text.",
  paperclipGetIssue: "Get one Paperclip task/issue with description, assignment, dependencies, status, and execution details.",
  paperclipGetHeartbeatContext: "Get compact execution, continuation, blocker, and wake context for one Paperclip task/issue.",
  paperclipListComments: "List comments for one Paperclip task/issue, with optional incremental pagination.",
  paperclipListIssueInteractions: "List issue interactions with status, resolver policy, payload, and result. Use this before attempting to resolve a pending interaction.",
  paperclipListIssueApprovals: "List formal approvals linked to one accessible Paperclip task/issue. This is read-only.",
  paperclipGetApproval: "Get one formal approval, including its status and decision context. This is read-only.",
  paperclipListProjects: "List Paperclip projects available for task planning, assignment, and workspace selection.",
  paperclipGetProject: "Get details and workspace information for one Paperclip project.",
  paperclipListGoals: "List company goals available for planning and linking Paperclip tasks.",
  paperclipGetIssueWorkspaceRuntime: "Get the current workspace and runtime services for one Paperclip task/issue.",
  paperclipControlIssueWorkspaceServices: "Start, stop, or restart runtime services for one Paperclip task workspace.",
  paperclipWaitForIssueWorkspaceService: "Wait for one Paperclip workspace service to become running and expose a URL when available.",
  paperclipCreateIssue: "Create a Paperclip task/issue with optional assignment, priority, project, goal, dependencies, and execution settings.",
  paperclipUpdateIssue: "Update a Paperclip task/issue including status, owner, priority, dependencies, execution settings, or comment.",
  paperclipAddComment: "Add a comment or instruction to one Paperclip task/issue, optionally resuming or interrupting execution.",
  paperclipResolveIssueInteraction: "Accept, reject, respond to, or cancel an issue interaction. Human-only and cancel actions require Board credentials; the API enforces resolver policy.",
  paperclipApprovalDecision: "Approve, reject, request revision, or resubmit a formal approval. The first three require Board credentials; resubmit is available to the requester.",
  paperclipCancelHeartbeatRun: "Cancel an active heartbeat run. Board-only: requires Board credentials, preserves operator provenance, and is audited by the API.",
  paperclipListDocuments: "List documents attached to one accessible Paperclip issue, including current revision metadata. This is read-only.",
  paperclipGetDocument: "Get the current contents and metadata for one accessible Paperclip issue document. This is read-only.",
  paperclipGetDocumentHistory: "List version history for one accessible Paperclip issue document. This is read-only.",
  paperclipGetDocumentRevision: "Get one specific revision of an accessible Paperclip issue document. This is read-only.",
  paperclipUpdateDocument: "Create a new document revision only when baseRevisionId matches the current revision. Returns DOCUMENT_REVISION_CONFLICT instead of overwriting a concurrent change.",
  paperclipWikiList: "List safe, editable Wiki pages with descriptions, current hashes, and recommended skills. It never returns raw sources or runtime files.",
  paperclipWikiSearch: "Search safe Wiki page metadata and return compact matches without returning every page body. This is read-only.",
  paperclipWikiGetPage: "Read one safe Wiki markdown page and its current hash. It rejects raw, template, runtime, and traversal paths.",
  paperclipWikiGetMetadata: "Get safe Wiki page metadata, including description, type, hash, editability, and recommended skills. This is read-only.",
  paperclipWikiProposeChange: "Prepare a non-mutating Wiki page replacement and diff after verifying expectedHash. It creates a proposal but never writes the Wiki.",
  paperclipWikiApplyChange: "Apply a previously prepared Wiki proposal only when expectedHash still matches the page. Returns WIKI_HASH_CONFLICT instead of overwriting concurrent edits.",
  paperclipListSkills: "List available Paperclip skills with names, descriptions and aliases. Use to discover which skill matches the user request before loading it.",
  paperclipGetSkill: "Get the full SKILL.md content for one Paperclip skill by name. Use after listing to load detailed instructions.",
  paperclipUseSkill: "Route a request through a Paperclip skill deterministically and return its execution envelope. Aliases map /debug /fix-tools /health /coo /runtime to skills.",
  paperclipListIssueRuns: "List all known Paperclip and correlated OpenCode runs for one accessible issue. This is read-only.",
  paperclipGetRunEvents: "Get ordered, persisted execution events for one accessible run. Unsupported telemetry is explicit. This is read-only.",
  paperclipGetRunMetrics: "Get deterministic metrics from persisted execution telemetry for one accessible run. Unsupported fields are null. This is read-only.",
  paperclipListSecrets: "List company secret metadata only. Raw secret values are never returned.",
  paperclipCreateSecret: "Create a company secret through the Board API. Values are redacted from all tool output.",
  paperclipUpdateSecret: "Update safe metadata or provider references for a company secret. The Board API audits the change.",
  paperclipRotateSecret: "Rotate a company secret. Input values and returned secret material are redacted.",
  paperclipDeleteSecret: "Delete a company secret through the Board API. This is audited and never returns secret material.",
  paperclipListToolApplications: "List configured App/MCP applications for one company.",
  paperclipCreateToolApplication: "Create an App/MCP application. Board credentials and API policy are required.",
  paperclipUpdateToolApplication: "Update one App/MCP application. The Board API validates and audits the mutation.",
  paperclipListToolConnections: "List safe connection metadata for configured App/MCP applications.",
  paperclipGetToolConnection: "Get safe metadata, health, and catalog state for one App/MCP connection.",
  paperclipCreateToolConnection: "Create an App/MCP connection using credential secret references, never plaintext credentials.",
  paperclipUpdateToolConnection: "Update an App/MCP connection using safe configuration and credential references.",
  paperclipTestToolConnection: "Run a Board-audited connection health and protocol probe with sanitized diagnostics.",
  paperclipRefreshToolConnectionCatalog: "Refresh a connection tool catalog through the Board API and record an audit event.",
  paperclipListToolProfiles: "List App/MCP access profiles for one company.",
  paperclipGetToolProfile: "Get one App/MCP access profile and its safe bindings.",
  paperclipCreateToolProfile: "Create an App/MCP access profile. Board credentials and API policy are required.",
  paperclipUpdateToolProfile: "Update an App/MCP access profile. The Board API audits the mutation.",
  paperclipBindToolProfile: "Bind an App/MCP profile to a company or agent. The Board API enforces scope.",
  paperclipSetAgentAppPermission: "Set allowed app access separately from every-run installation. Board API policy is enforced.",
  paperclipSetAgentAppInstallPolicy: "Set every-run app installation separately from permission. Board API policy is enforced.",
  paperclipListToolGateways: "List named MCP gateway metadata for one company without exposing gateway tokens.",
  paperclipCreateToolGateway: "Create a named MCP gateway. Gateway bearer-token issuance is intentionally unavailable.",
  paperclipUpdateToolGateway: "Update named MCP gateway metadata. The Board API validates and audits the mutation.",
  paperclipGetExecutionWorkspaceDelivery: "Get workspace delivery state, branch metadata, and pull-request readiness.",
  paperclipPrepareIssueDelivery: "Fetch and preflight workspace delivery without changing its branch. Board credentials are required.",
  paperclipCreateIssuePullRequest: "Create a GitHub pull request from a preflighted workspace with no PAT input.",
  paperclipMergeIssuePullRequest: "Merge a preflighted GitHub pull request with exact head-SHA verification and Board authorization.",
};

export function filterChatGptPublicTools(tools) {
  const byName = new Map(tools.map((tool) => [tool.name, tool]));
  return CHATGPT_PUBLIC_TOOL_NAMES.flatMap((name) => {
    const tool = byName.get(name);
    return tool ? [{ ...tool, description: CHATGPT_PUBLIC_TOOL_DESCRIPTIONS[name] }] : [];
  });
}

const companyId = { type: "string", format: "uuid", description: "Optional selection from the authenticated principal's granted companies." };

export const PUBLIC_GATEWAY_TOOLS = [
  ["paperclipListIssueRuns", { issueId: { type: "string" } }, ["issueId"]],
  ["paperclipGetRunEvents", { runId: { type: "string" }, kind: { type: "string" }, cursor: { type: "integer", minimum: 0 }, limit: { type: "integer", minimum: 1, maximum: 200 } }, ["runId"]],
  ["paperclipGetRunMetrics", { runId: { type: "string" } }, ["runId"]],
  ["paperclipGetDocumentHistory", { issueId: { type: "string" }, key: { type: "string" } }, ["issueId", "key"]],
  ["paperclipGetDocumentRevision", { issueId: { type: "string" }, key: { type: "string" }, revisionId: { type: "string" } }, ["issueId", "key", "revisionId"]],
  ["paperclipUpdateDocument", { issueId: { type: "string" }, key: { type: "string" }, baseRevisionId: { type: "string" }, content: { type: "string" }, title: { type: "string" }, changeSummary: { type: "string" } }, ["issueId", "key", "baseRevisionId", "content"]],
  ["paperclipWikiList", { companyId, spaceSlug: { type: "string" } }, []],
  ["paperclipWikiSearch", { companyId, spaceSlug: { type: "string" }, query: { type: "string" }, limit: { type: "integer", minimum: 1, maximum: 50 } }, ["query"]],
  ["paperclipWikiGetPage", { companyId, spaceSlug: { type: "string" }, page: { type: "string" } }, ["page"]],
  ["paperclipWikiGetMetadata", { companyId, spaceSlug: { type: "string" }, page: { type: "string" } }, ["page"]],
  ["paperclipWikiProposeChange", { companyId, spaceSlug: { type: "string" }, page: { type: "string" }, expectedHash: { type: "string" }, content: { type: "string" }, summary: { type: "string" } }, ["page", "expectedHash", "content"]],
  ["paperclipWikiApplyChange", { proposalId: { type: "string" }, expectedHash: { type: "string" } }, ["proposalId", "expectedHash"]],
].map(([name, properties, required]) => ({
  name,
  description: CHATGPT_PUBLIC_TOOL_DESCRIPTIONS[name],
  inputSchema: { type: "object", properties, required, additionalProperties: false, "$schema": "http://json-schema.org/draft-07/schema#" },
}));
